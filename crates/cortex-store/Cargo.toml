[package]
name = "cortex-store"
version = "0.1.0"
edition = "2021"

[dependencies]
cortex-core = { path = "../cortex-core" }
rusqlite = { version = "0.32", features = ["bundled"] }
tracing = { workspace = true }
